// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   /* config options here */
// };

// export default nextConfig;


/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "ranamahacreations.com",
      "w7.pngwing.com",
      "upload.wikimedia.org",
      "seeklogo.com"
    ],
  },
};

export default nextConfig;